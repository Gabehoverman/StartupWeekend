@model Startup_Weekend_Application.Models.Discover
@{
    ViewData["Title"] = "New";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>New</h2>
@using (Html.BeginForm("Add", "Discover", FormMethod.Post, new { @class = "form" }))
{
    <div class="form-group">
        @Html.LabelFor(m => m.Game)
        @Html.TextBoxFor(m => m.Game, new { @class = "form-control" })
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Username)
        @Html.TextBoxFor(m => m.Username, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Platform)
        @Html.TextBoxFor(m => m.Platform, new { @class = "form-control" })
    </div>

    /*<div class="form-group">
        @Html.LabelFor(m => m.Playstyle)
        @Html.TextBoxFor(m => m.Playstyle, new { @class = "form-control" })
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Genre)
        <select asp-for="Genre" asp-items="Html.GetEnumSelectList<Enums.Genre()"></select>

    </div> */
    <input type="submit" class="btn btn-primary" value="Save" />
}
